# 另一台电脑部署说明

这个压缩包包含网站完整源码、页面素材、腾讯云 COS 上传功能和 GitHub Pages 自动部署配置。

## 一、本地运行

另一台电脑需要先安装 Node.js 20 或更高版本，然后解压压缩包，在网站目录打开 PowerShell：

```powershell
npm install
npm run dev -- --host
```

浏览器访问终端显示的地址。因为当前 GitHub 仓库名是 `shipinduanjuwangzhan`，本地地址通常为：

```text
http://localhost:5173/shipinduanjuwangzhan/
```

如需在本地网页上传图片或视频，请在腾讯云 COS 的 CORS 规则中额外添加：

```text
http://localhost:5173
```

## 二、部署到新的 GitHub 仓库

1. 在新电脑登录 GitHub，并创建新仓库。
2. 如果新仓库不叫 `shipinduanjuwangzhan`，修改 `vite.config.mjs` 中的 `base`：

```js
base: "/新仓库名/",
```

3. 如果继续使用现在的腾讯云 COS，无需修改 `src/cosAssets.js`，现有图片、视频和文字内容会继续从同一个 COS 桶读取。
4. 如果使用新的腾讯云 COS，修改 `src/cosAssets.js` 顶部的 `COS_BUCKET` 和 `COS_REGION`。
5. 推送代码到新 GitHub 仓库，在仓库 `Settings → Pages` 中把 Source 设置为 `GitHub Actions`。
6. 在腾讯云 COS 中把新网站来源加入 CORS，例如：

```text
https://新GitHub用户名.github.io
```

允许 Methods：`GET, PUT, POST, HEAD`；Allow-Headers：`*`；Expose-Headers：`ETag`。

## 三、内容管理

腾讯云 `SecretId` 和 `SecretKey` 只保存在浏览器中，不会进入压缩包。换电脑后，第一次点击网站右下角齿轮时，需要重新输入 COS 密钥并保存。

只要继续使用当前 COS 桶，访客看到的项目、视频、图片和可编辑文字会与当前网站一致。
